// ==================== 页面参数规则 ====================
// TypeScript（运行前检查数据形状的工具）会按这里的规则检查每一次页面跳转。
// 这里只说明页面需要哪些数据，不负责显示页面或执行跳转。

// ---------- 底部标签参数 ----------
// 两个主页面目前不接收额外数据，切换标签只会改变当前显示的页面。
export type MainTabParamList = {
  Fasting: undefined;
  Statistics: undefined;
};
