// ==================== 禁食页面 ====================
// 当前先保留一个能运行的页面外壳，下一阶段再按 design/fasting 中的设计稿实现计时界面。

import {Screen} from '../../../shared/components/Screen';

export function FastingScreen() {
  return <Screen title="禁食" extendUnderBottomBar />;
}
