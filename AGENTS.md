# NutriTime 全仓库开发规则

## 1. 文件级别与适用范围

本文件位于仓库根目录，是整个 NutriTime 项目的最高层级开发规则，适用于本目录及所有子目录中的代码、测试、脚本、配置和文档。

子目录可以通过自己的 `AGENTS.md` 增加更具体的技术规则，但不得删除、绕过或降低本文件要求。若不同层级的规则发生冲突，应在遵守系统指令和用户当前明确要求的前提下，采用对正确性、兼容性、可读性和中文注释要求更严格的规则。

开始任何分析、修改、重构或新增功能前，必须先完整阅读本文件，再阅读目标目录中可能存在的更具体规则。

## 2. 项目背景

NutriTime 是一个 Android 手机与 Wear OS 轻断食计时项目，目前主要包含以下目录：

- `apps/mobile/`：React Native + TypeScript + Expo Development Build 手机端；`android/` 由 Expo Prebuild 按配置生成。
- `apps/wear/`：Kotlin + Jetpack Compose for Wear OS 工程位置，目前只有阶段说明。
- `design/`：用户已确认的手机与手表视觉设计来源。
- `docs/`：实施计划、产品范围、环境、签名、同步协议和测试文档。

各工程的依赖、构建命令和环境配置相互独立。不得在仓库根目录混装子工程依赖，也不得为了完成一个子工程的需求而无理由修改其他工程。

### 2.1 NutriTime 技术与产品边界

1. 手机端已经采用 React Native + TypeScript + Expo Development Build；不得退回以 React Native CLI 或 Expo Go 为主的开发方式。
2. `apps/mobile/android/` 后续由 Expo Prebuild 生成，不得成为重要 Kotlin 业务逻辑的唯一保存位置。
3. Wear Data Layer 原生逻辑必须放在 `apps/mobile/modules/wear-data-layer/` 本地 Expo Module；Manifest 和权限优先使用 app config、官方配置或 config plugin。
4. 阶段 7 前只实现 16 小时断食闭环；8 小时进食窗口属于阶段 8。阶段 8 前 README 必须明确当前不是完整 16:8 周期。
5. 不保存 `remainingSeconds`；倒计时由开始和计划结束时间戳派生。禁止每秒存储或每秒跨设备同步。
6. 第一阶段手机是唯一真相来源。手表遇到未知或非法协议时保留 last good state 并显示错误，不得静默回到 idle。
7. 不提前加入后端、登录、历史、Tile、卡路里、体重、双向同步或冲突框架。
8. Release 密钥、密码、令牌和私有凭据不得提交。Debug keystore 只能用于本地调试。
9. 手机和手表 applicationId 固定为 `com.zensoku.nutritime`；mobile versionCode 从 `1000001` 起，wear 从 `2000001` 起。

## 3. 核心目标与优先级

所有工作按照以下顺序处理：

1. 正确满足用户当前提出的需求。
2. 保证代码安全，不泄露密钥、账号、隐私数据或其他敏感信息。
3. 保持现有公开接口、输入输出、默认行为和错误处理方式兼容。
4. 让完全没有编程基础的读者也能通过清晰的代码结构和关键中文注释，理解代码为什么存在、如何运行以及修改时要注意什么。
5. 优先复用项目中已有的组件、函数、类型、工具和实现方式。
6. 使用最小范围、最少必要代码完成需求，不顺手重构无关内容。
7. 保持代码清晰、可测试、可维护，不为尚未提出的未来需求提前设计。

若“减少代码量”和“让初学者看懂”发生冲突，应优先选择结构清楚、步骤明确、注释充分的写法，但不能因此复制大量相同逻辑或破坏性能与正确性。

## 4. 中文单行注释规范

### 4.1 目的与基本原则

注释用于帮助读者理解代码无法直接表达的业务原因和限制，同时提高在 IDE（集成开发环境，也就是编写代码的软件）中的辨识度。新增或修改代码时，应检查相关关键逻辑是否需要中文注释，但不得为了增加注释数量而逐行翻译代码。

注释重点说明以下内容：

- 为什么要这样处理。
- 特殊业务规则和适用条件。
- 容易产生歧义的状态、金额、时间、权限或数据关系。
- 边界情况（例如金额为空或网络突然断开）、旧版本兼容方式和容易被误删的保护措施。
- 等待网络或文件操作时的执行顺序、页面打开与关闭时的处理，以及速度或安全方面不容易看出来的取舍。
- 采用非常规实现的原因，以及后续修改时不能破坏的约束。

若代码通过清楚的命名和简单结构已经能够准确表达含义，不要添加“设置变量”“判断状态”“返回结果”这类重复代码表面的注释。

### 4.2 “奶奶也能看懂”的标准

注释必须使用日常说话方式，让从未学过编程的人不用猜测就能理解。不能只把专业术语换成另一个专业术语，也不能假设读者已经知道框架的默认行为。

对关键流程的说明应根据实际需要回答以下问题：

1. 什么时候会执行这段代码，是用户点了按钮，还是系统自动处理？
2. 数据从哪里来，例如来自输入框、手机本地记录还是服务器？
3. 为什么要经过这一步，省略后会发生什么具体问题？
4. 处理完成后，数据保存到了哪里，或者用户会看到什么变化？
5. 遇到没有数据、重复点击、网络失败或页面关闭时会怎样处理？

写注释时必须遵守以下表达要求：

- 使用短句，一句话尽量只说明一件事。
- 每个文件中第一次出现专业术语时，立即用括号或同一句话解释成生活化中文。
- 必须保留英文名称时，先写它在代码中的原名，再解释它实际负责什么。
- 金额、时间、数量和状态要说明单位或真实含义；容易误解时给出一个简单例子。
- 框架自动完成但代码中看不出来的行为，应在第一次依赖该行为的位置解释一次，后面相同用法不重复说明。
- 不使用“显然”“很简单”“顾名思义”等说法，避免让初学者因为看不懂而产生压力。

例如，不能只说“更新 state”，因为零基础读者可能不知道 `state` 是什么。应写成：

```ts
// useState（组件自己的小记事本）会保存用户刚输入的金额。
// 用户每输入一个数字，amount 都会更新，页面也会重新显示最新金额。
const [amount, setAmount] = useState('');
```

“奶奶也能看懂”不等于给每个括号、等号或变量重复写解释。能通过清楚命名直接看懂的代码不加废话；真正影响业务结果、执行顺序或异常处理的地方必须把前因后果说清楚。

### 4.3 三级注释结构

项目统一使用以下三级注释结构：

```ts
// ==================== 一级模块 ====================

// ---------- 二级模块 ----------

// 具体业务说明
```

一级模块注释用于分隔较大的业务模块，例如记账、分类管理、统计报表或账户设置：

```ts
// ==================== 账目保存 ====================
```

二级模块注释用于分隔同一模块内部不同的业务逻辑，例如输入校验、金额计算或失败处理：

```ts
// ---------- 输入校验 ----------
```

普通业务注释紧邻相关代码，直接说明当前规则存在的原因：

```ts
// 金额统一换算成最小货币单位后再计算，避免小数运算产生精度误差。
const amountInCents = Math.round(amount * 100);
```

只有代码中确实存在对应的模块或子模块时才添加标题。不得为了形式统一而创建空标题，也不得在几行简单代码之间反复插入标题，影响连续阅读。

### 4.4 模块、函数与关键逻辑

新增较大的人工维护源代码文件时，应在文件顶部使用一级模块注释标明文件的主要职责。必要时紧接一至三行普通注释，说明入口、关键输入输出或容易改坏的约束，不要写成长篇文件说明。

函数、类、组件、状态、接口、数据库操作和测试代码，只有在名称与结构不足以说明意图，或者存在特殊规则时才添加普通业务注释。注释应放在相关代码之前，并让零基础读者能够理解这条规则对应的真实使用场景。

不常见的框架行为、正则表达式（按规则查找文字的特殊写法）、异步竞态（多个任务完成顺序不固定导致互相覆盖）、事务边界（一组数据库操作必须全部成功或全部取消）、安全保护和速度优化必须解释“为什么”。测试注释重点说明特殊场景、非常规数据准备和需要防止的回归问题（已经修好的错误再次出现），不要复述测试步骤或检查结果的代码。

### 4.5 普通业务注释示例

不推荐：

```ts
// 判断状态是否为已同步。
if (status === SYNCED) {
  // 返回。
  return;
}
```

上面的注释只是换一种说法重复代码，没有说明业务原因。

推荐：

```ts
// 已同步账目由服务端作为最终版本，本地不再重复提交，避免生成两条相同记录。
if (status === SYNCED) {
  return;
}
```

### 4.6 多条规则与注释长度

同一位置存在多条业务规则时，继续使用单行注释，并通过列表组织：

```ts
// 账目同步规则：
// - 没有网络时保留在本地等待下次同步。
// - 服务端已存在相同编号时更新原记录，不新增重复记录。
// - 同步成功后记录服务端编号，后续修改使用该编号定位数据。
```

单段注释尽量控制在 5～8 行以内。如果需要说明大量业务背景、完整流程、多种产品场景或历史原因，说明内容已经不适合全部放在代码中。

代码中只保留关键规则摘要，并使用一至两行单行注释指向仓库内真实存在的 `README` 或 `docs` 文档。完整规则在文档中维护，引用路径必须准确，不能指向不存在或无法访问的内容。

```ts
// 完整同步冲突规则见：docs/architecture.md。
// 当前代码只负责合并具有相同服务端编号的账目。
```

### 4.7 单行注释格式

TypeScript、JavaScript、Java、Kotlin 和 Swift 统一使用 `//`。其他语言使用自身的单行注释符号，例如 Python 和 Shell 使用 `#`，SQL 使用 `--`。

不使用 `/* ... */`、`/** ... */` 等块注释或多行文档注释。说明内容较长时，每一行都重新写单行注释符号。函数、类和公开接口也遵守这项规则。

如果文件格式没有真正的单行注释语法，但允许把合法注释写在一个物理行内，例如 HTML 或 XML 的 `<!-- 中文说明 -->`，则每条注释只能占一个物理行。对于 JSON 等完全不支持注释的格式，不得插入非法注释；必要说明放在同目录的 README、示例配置或 `docs` 文档中。

依赖锁文件、编译产物、自动生成代码、第三方代码、图片和二进制资源不手工添加注释。需要调整自动生成内容时，应修改其源文件或生成配置。

### 4.8 注释维护与代码清晰度

修改代码时，必须同时检查附近注释是否仍然准确。过时、与实现矛盾或指向失效文档的注释必须一并更新。

不得复制粘贴通用说明来应付注释要求，也不得写无法从需求、代码、测试或项目文档中确认的猜测。不确定业务含义时，先查找调用方和相关文档，仍无法确认再向用户说明。

注释不能代替清晰代码。应先使用准确命名、短小步骤和符合项目习惯的结构，再用三级单行注释说明代码本身无法表达的背景和原因。

## 5. 实现与设计约束

### 5.1 修改前必须理解上下文

开始编码前，必须先完成以下检查：

1. 阅读目标文件及其直接调用方和被调用方。
2. 查找项目中是否已有相同或相近实现。
3. 阅读相关类型、测试、接口定义和项目文档。
4. 确认目标子工程已有的运行、测试和格式化命令。
5. 检查当前 Git 工作区，区分用户已有修改与本次任务修改。

不得覆盖、回退、格式化或删除用户已有但与当前任务无关的修改。

### 5.2 最小范围实现

修改应局限在当前需求直接相关的文件和代码路径内。不得顺手执行以下操作：

- 重构无关代码。
- 调整无关格式或 import 顺序。
- 重命名无关变量、文件或目录。
- 升级无关依赖。
- 改变未被要求修改的默认行为。
- 添加仅供未来可能使用的配置、抽象层或扩展点。

发现无关问题时，可以在最终回复中指出，但不能擅自修复。

### 5.3 复用优先

新增实现前必须先搜索项目已有的组件、工具函数、类型、错误处理、接口封装和测试辅助方法。能够直接复用时不得重复实现；能够通过小范围扩展现有代码解决时，不得另起一套并行方案。

只有同时满足以下至少一项时才允许新增抽象：

1. 能明显减少当前需求造成的重复代码。
2. 能降低当前实现的真实复杂度。
3. 与项目现有架构和命名风格一致。
4. 当前需求没有该抽象就无法清晰、正确地实现。

不得以“以后可能会用到”为理由提前设计。

### 5.4 兼容性

修改现有功能时，必须优先保持：

- 公开接口和函数签名兼容。
- 输入输出数据结构兼容。
- 默认值和默认行为兼容。
- 错误类型、错误提示和错误处理方式兼容。
- 已有调用方不需要同步修改。
- 已持久化数据和旧版本客户端仍能正常使用。

如果需求确实要求破坏兼容性，编码前必须向用户明确说明原因、影响范围、迁移方式和可选替代方案，得到确认后才能实施。

### 5.5 依赖与安全

能使用标准库或项目现有依赖完成时，不得新增依赖。确需新增依赖时，必须说明必要性、维护状态、许可证和对包体积或运行环境的影响，并使用该子工程现有的包管理工具安装。

禁止提交真实密钥、访问令牌、密码、私有地址、个人身份信息或生产数据。示例值必须明显是虚构占位符。日志和错误信息不得无意输出敏感内容。

## 6. 测试与验证

每次修改完成后，都必须按照风险选择并实际执行验证：

1. 文档或注释修改：检查内容、链接、格式和术语是否一致。
2. 小范围逻辑修改：运行相关单元测试、类型检查或最小可验证命令。
3. 跨模块修改：运行所有受影响模块的测试和构建检查。
4. 用户可见行为修改：验证主要使用路径、加载状态、空状态、失败状态和重复操作。
5. 数据、权限、支付或安全相关修改：额外验证边界条件、失败回滚和越权场景。

新增或修复业务行为时，应同时补充能够证明该行为的测试。测试代码同样遵守中文单行注释规范；只说明特殊场景、非常规数据准备和需要防止的回归问题（也就是已经修好的错误再次出现），不重复描述清楚的测试步骤或检查结果。

不得声称运行过实际上没有运行的测试。如果由于环境、依赖、权限或外部服务原因无法运行，必须在最终回复中说明具体原因，并列出已经完成的静态检查或人工核对。

## 7. 工作完成后的回复要求

完成任务后，必须用简明、通俗的中文向用户说明：

1. 修改了哪些文件和功能。
2. 为什么采用当前实现。
3. 添加了哪些帮助初学者理解的注释。
4. 实际运行了哪些验证命令，以及结果如何。
5. 是否存在未解决风险、无法验证的部分或后续事项。

回复中第一次出现专业术语时，要顺带用日常语言解释。不得只给出文件列表、测试命令或“已完成”三个字。

## 8. 提交前自检清单

在认为任务完成前，逐项确认：

- [ ] 已完整阅读根目录和目标目录中的 `AGENTS.md`。
- [ ] 修改范围只覆盖当前需求，没有动用户的无关改动。
- [ ] 已搜索并优先复用项目现有实现。
- [ ] 新增或修改代码中的关键业务规则和非直观逻辑都有面向零基础读者的中文注释。
- [ ] 注释通过“奶奶也能看懂”检查：使用日常短句，讲清数据来源、处理原因、最终结果和失败情况。
- [ ] 每个文件首次出现的专业术语、英文缩写和框架自动行为都有紧邻的生活化中文解释。
- [ ] 注释按照一级模块、二级模块和普通业务说明三级结构组织，没有滥用标题分隔简单代码。
- [ ] 所有人工注释均使用单行注释；较长说明逐行重复注释符号，没有使用块注释或多行文档注释。
- [ ] 单段注释尽量不超过 5～8 行；复杂背景已转移到真实存在的 README 或 `docs` 文档。
- [ ] 注释解释了业务目的、实现原因、关键流程、边界情况和修改风险，而不是逐字翻译代码。
- [ ] 专业术语和英文缩写在首次出现时有中文解释。
- [ ] 注释与最终代码完全一致，没有保留过时说明。
- [ ] 不支持注释的文件没有被强行加入非法语法，并提供了必要的替代说明。
- [ ] 没有引入不必要的抽象、依赖、配置或兼容性破坏。
- [ ] 已运行与改动风险匹配的测试或检查，并如实记录结果。
- [ ] 最终回复能让非技术用户理解本次修改的内容、原因和验证情况。
